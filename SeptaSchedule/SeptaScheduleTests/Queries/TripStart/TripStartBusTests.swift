// Septa. 2017

import Foundation
import XCTest
@testable import SeptaSchedule

class TripStartBusTests: XCTestCase {

    //
    //        expectedResult.sort { $0.stopId < $1.stopId
    //            }
    //
    //            let route = Route(routeId: "33", routeShortName: "", routeLongName: "")
    //
    //            tripStartCommand.tripStarts(forTransitMode: .bus, route: route) { stops, error in
    //                var actualResult = stops!
    //                actualResult.sort { $0.stopId < $1.stopId }
    //                XCTAssertEqual(expectedResult, actualResult)
    //                XCTAssertNil(error)
    //                expectation.fulfill()
    //            }
    //            waitForExpectations(timeout: 2) { error in
    //                if let error = error {
    //                    XCTFail("waitForExpectationsWithTimeout errored: \(error)")
    //                }
    //            }
}
